// Generated by CoffeeScript 1.6.3
var Vertex;

Vertex = (function() {
  function Vertex(name, content, children, repeatability) {
    this.name = name;
    this.content = content;
    this.children = children;
    this.repeatability = repeatability != null ? repeatability : 1;
    this.tree_edge;
    this.forward_edge;
    this.back_edge;
    this.cross_edge;
  }

  return Vertex;

})();

test('Vertex Basics', function() {
  var vertex_A, vertex_B, vertex_C;
  vertex_B = new Vertex('B', 'b', null, null);
  vertex_C = new Vertex('C', 'c', null, null);
  vertex_A = new Vertex('A', 'a', null, null);
  vertex_A.children = [[vertex_B], [vertex_C]];
  equal(vertex_A.name, 'A');
  equal(vertex_A.content, 'a');
  equal(vertex_A.repeatability, 1);
  equal(vertex_A.children.length, 2);
  equal(vertex_A.children[0][0], vertex_B);
  return equal(vertex_A.children[1][0], vertex_C);
});
